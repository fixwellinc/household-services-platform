export const metadata = {
  title: 'Admin Dashboard | Household Services',
  description: 'Admin dashboard for managing quotes, replying to requests, and sending marketing emails.',
};

export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return children;
} 